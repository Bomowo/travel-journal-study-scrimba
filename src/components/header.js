import logo from "../resource/Fill 213.svg"

export default function Header () {
    return (
        <nav>
            <img src={logo}></img>
            <span>my travel journal.</span>
        </nav>
    )
}